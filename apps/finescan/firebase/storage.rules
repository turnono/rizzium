rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Temporarily allow all access to debug the upload issue
      allow read, write: if true;
    }
  }
}

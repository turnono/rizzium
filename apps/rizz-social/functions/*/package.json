{
  "name": "${function-name}",
  "description": "Firebase Function, auto generated by @simondotm/nx-firebase",
  "scripts": {
    "test": "nx test ${function-name}",
    "lint": "nx lint ${function-name}",
    "build": "nx build ${function-name}",
    "deploy": "nx deploy ${function-name}"
  },
  "type": "module",
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.1.0"
  },
  "private": true
}
